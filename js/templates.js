const templates = {

    modern: (state) =>
        renderModernTemplate(state),

    minimal: (state) =>
        renderMinimalTemplate(state),

    academic: (state) =>
        renderAcademicTemplate(state)

};